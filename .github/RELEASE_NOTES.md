# V4.5.0 Nargacuga 🌙

1. 搜索改到后台进行，笔记很多时输入也不会卡住，搜「.」或「md」不再把所有笔记都列出来
2. iPhone 上打开笔记不会再悄悄改动它的修改时间，废纸篓里的笔记不再混进最近和搜索，界面补齐了中文，GBK 编码的笔记也能打开
3. 默认字体回到仓耳今楷，没装时先用苹方显示，在字体设置里点下载，装好后自动换上，之前选过的字体都保持不变
4. 代码字体默认跟随正文，只用在代码块上，行内代码、引用里的代码和 img 这类 HTML 标签不再换成等宽字体，iPhone 上也一样
5. 修复 4.3.1 起预览文字变细、标题加粗变淡的问题，预览、编辑器和笔记列表的字间距也收紧了一些
6. 设置窗口重新整理，控件对齐、分组更清楚，窗口高度跟着每页内容走，字体那一页改叫「字体」
7. 预览里的查找按普通文字匹配，输入 .、( 这类符号不再出错

---

1. Search runs in the background, so typing stays smooth in large libraries, and searching for "." or "md" no longer lists every note
2. On iPhone, opening a note no longer changes its modified date, trashed notes stay out of Recent and search, the interface is fully translated into Chinese, and GBK-encoded notes open
3. TsangerJinKai02 is the default font again: PingFang shows until it is installed, Settings can download it, it takes over on its own once installed, and fonts you already chose stay as they are
4. The code font follows the text font by default and applies only to code blocks, so inline code, quoted code and HTML tags such as img keep the text font, on iPhone too
5. Fixes preview text and bold headings looking thinner since 4.3.1, and tightens letter spacing in the preview, editor and note list
6. Settings is reorganized with aligned controls, clearer groups, a window that fits each page, and a page named Fonts
7. Find in preview matches plain text, so symbols like . and ( no longer break it
